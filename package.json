{
  "name": "herodotus-evm-v2",
  "version": "2.0.0",
  "repository": "git@github.com:HerodotusDev/herodotus-evm-v2.git",
  "author": "Herodotus Ltd <hello@herodotus.dev>",
  "license": "GPL-3.0",
  "scripts": {
    "preinstall": "npx only-allow bun",
    "prettier": "prettier --write --list-different ./*",
    "prettier:check": "prettier --check .",
    "forge:build": "forge build && bun forge:selectors",
    "forge:test": "forge test",
    "forge:anvil": "anvil",
    "forge:selectors": "bun ts-helpers/get-all-function-selectors.ts",
    "forge:deploy": "source .env && export PRIVATE_KEY=$ANVIL_PRIVATE_KEY && forge script script/deploy/Satellite.s.sol:Deploy",
    "forge:deploy:anvil": "source .env && export PRIVATE_KEY=$ANVIL_PRIVATE_KEY && forge script script/deploy/Satellite.s.sol:Deploy --fork-url $ANVIL_RPC_URL --broadcast -vvvv",
    "forge:deploy:sepolia": "source .env && forge script --chain sepolia script/deploy/Satellite.s.sol:Deploy --rpc-url $SEPOLIA_RPC_URL --broadcast --verify",
    "forge:deploy:update": "source .env && export PRIVATE_KEY=$ANVIL_PRIVATE_KEY && forge script",
    "forge:deploy:update:anvil": "source .env && export PRIVATE_KEY=$ANVIL_PRIVATE_KEY && forge script --fork-url $ANVIL_RPC_URL --broadcast -vvvv",
    "forge:deploy:update:sepolia": "source .env && forge script --chain sepolia --rpc-url $SEPOLIA_RPC_URL --broadcast --verify"
  },
  "devDependencies": {
    "only-allow": "^1.2.1",
    "prettier": "^3.3.3",
    "prettier-plugin-solidity": "^1.4.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
